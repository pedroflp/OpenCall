# Ícones

Só HugeIcon. Nada de lucide-react, Heroicons, phosphor, emoji como ícone ou
SVG próprio. Use `<HugeIcon name="..." size={n} />` (`@/components/HugeIcon`)
sempre — regra completa em `src/flows/channel/text/CLAUDE.md`.
